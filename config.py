"""Runtime configuration shared between main.py and handlers.py.

Filled in by main() at startup, then used as the default subnet/community
whenever a scan is triggered without explicit parameters.
"""

CONFIG = {}
